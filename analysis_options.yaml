include: package:flutter_lints/flutter.yaml

analyzer:
  exclude:
    # v1 sources kept only as a porting reference (see DECISIONS ADR-015).
    - legacy/**

linter:
  rules:
    - unawaited_futures
    - prefer_final_locals
    - cancel_subscriptions
    - close_sinks
    - avoid_print
