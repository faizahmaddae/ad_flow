include: ../analysis_options.yaml

# Test code uses deliberate fire-and-forget patterns (e.g. setUp cleanup,
# stub callbacks) that don't benefit from unawaited_futures enforcement.
analyzer:
  errors:
    unawaited_futures: ignore
